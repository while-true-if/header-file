#include <stdio.h>
#include "" // "calc.c"'s path
#include "" // "calc.h"'s path

void main()
{
    int z = 10, y = 3;
    int x = spp(z,y);
    pri(5);
    prf(10.5);
    prd(10000.5);
    power(2, 1, 3);
    dia(103);
    dipo(5);
}
